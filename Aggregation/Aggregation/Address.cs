using System;
using System.Collections.Generic;
using System.Text;

namespace Aggregation
{
    class Address
    {
        public string City,District,Country;

        public Address(string city, string district, string country)
        {
            City = city;
            District = district;
            Country = country;
        }
    }
}
